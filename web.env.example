# ============================================
# Web Frontend Runtime Environment Variables
# ============================================
# These variables are injected at container startup
# via docker-entrypoint.sh

# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:3333

# WebSocket URL (usually same as API but with ws:// protocol)
NEXT_PUBLIC_WS_URL=ws://localhost:3333

# Default Tenant ID
NEXT_PUBLIC_TENANT_ID=tenant-alpha

# Next.js Server Port (internal)
PORT=3000
